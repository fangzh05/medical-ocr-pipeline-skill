interface:
  display_name: "Medical OCR Router"
  short_description: "Medical OCR routing and notation repair"
  default_prompt: "Use $medical-ocr-router to OCR this medical material into clean Markdown while preserving biomedical notation, tables, and formulas."

policy:
  allow_implicit_invocation: true

